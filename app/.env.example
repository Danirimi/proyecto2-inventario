# Copiar este archivo como .env en la VM (nunca se sube al repo, ver .gitignore)
# y completar con los valores reales del usuario creado en la Fase 1.

DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=inventario_app
DB_PASSWORD=Grupo6@
DB_NAME=inventario_db

PORT=3000